class Hello:
    def printHello(self):   # create method. method is a function that created inside the class.
        print("hello")
# this is the original way to create and call object

obj1= Hello()      # object creation. object name can be anything.
obj1.printHello()          # object call. printhello function is secured here coz only the object of that class can call it.

